<%= form_for(@solf) do |f| %>
  <% if @solf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solf.errors.count, "error") %> prohibited this solf from being saved:</h2>

      <ul>
      <% @solf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
